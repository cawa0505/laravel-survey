{% extends '../layouts/app.twig' %}

{% block content %}

    <h1>{{ group.text }}</h1>
    <h2>{{ group.small_text }}</h2>

    <p>{{ group.slug }}</p>

    {{ form_open({url: ['admin', 'surveys', survey.id, 'groups', group.id]}) }}
    	<input type="hidden" name="_method" value="DELETE">
		{{ form_submit('Delete') }}
	{{ form_close() }}

{% endblock %}